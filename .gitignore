# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# Generated locally on the Mac by materialize.py — not committed
PaymentEntries_*.xlsx
run.log

# The master ledger lives on the Mac (the agent reads it via MASTER_LEDGER_PATH).
# It holds real customer/payment data and must never be committed.
Master Ledger.xlsm
*.xlsm

# Local Gmail OAuth material — never commit tokens or client secrets
credentials.json
token_*.json
*.oauth.json
